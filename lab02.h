#pragma once
#include <ctime>    // для time()
#include <cstdlib>  // для srand(), rand()
#include <cstdint>  // для стандартных типов (на будущее)

// === ВАЖНО ===
// Так как в таблице методички только 20 вариантов, а у тебя 22,
// берём вариант 22 ≈ вариант 2 по циклу (22 mod 20 = 2).
// Вариант 2: тип long, размер 15, диапазон [-100, 100].

typedef long t_rand_val;   // тип элементов последовательности
const int N = 15;          // размер последовательности

// Функция генерации последовательности псевдослучайных чисел
// A — указатель на массив для хранения
// cnt — количество элементов
// L, H — нижняя и верхняя границы диапазона
void make_rnd_vals(t_rand_val* A, int cnt, t_rand_val L, t_rand_val H);
